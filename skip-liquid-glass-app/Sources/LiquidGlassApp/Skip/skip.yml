# Skip configuration for LiquidGlassApp module
#
# Kotlin dependencies and Gradle build options for this module can be configured here
#build:
#  contents:
#    - block: 'dependencies'
#      contents:
#        - 'implementation("androidx.compose.runtime:runtime")'

# this is a natively-compiled Skip Fuse module
skip:
  mode: 'transpiled'
  
  bridging:
      auto: true
    enabled: 'kotlincompat'

  build:
  contents:
    - block: 'dependencies'
      contents:


